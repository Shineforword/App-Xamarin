<?xml version="1.0" encoding="utf-8" ?>
<!--根元素:指定父类
    (1)每个xmal文件只有一个根元素,根元素用来指定xaml的父类
    (2)在根元素中
        1定义引用的命名空间
        2定义类名(x:Class = "")类名必须与c#文件中的类名保持一致,并且必须要有所属的命名空间
 xmlns="http://xamarin.com/schemas/2014/forms",引入Xamarin.Forms命名空间
 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"引入Xamarin规范命名空间
 x:Class="AppX.MainPage" 定义类名
-->

<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="AppX.MainPage">
    <!--xaml文件主要定义界面,每个xaml文件需绑定2个C#文件.cs/.g.cs-->
    <!--Content可以省略不写-->
    <ContentPage.Content>
            <!--Button 对象元素,对象元素是一个对象 常用view属性 / 旋转等 -->
        <Button Text="Button" BackgroundColor="Red"
                WidthRequest="100"
                HeightRequest="60"
                HorizontalOptions="Start"
                VerticalOptions="Start"
                Opacity="0.5"
                >
        </Button>
    </ContentPage.Content>
</ContentPage>

